<div class ="jumbotron">
	<div class="center" id="tag_cloud">
		<% tag_cloud Pin.tag_counts, %w[s m l] do |tag, css_class| %>
			<%= link_to tag.name, tag_path(tag.name), class: css_class %>
		<% end %>
	</div>
</div>